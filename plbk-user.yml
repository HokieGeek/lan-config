---
- name: Configure my user stuff
  tasks:
      - name: Install needed packages
        # TODO: vim, tmux, zsh

      - name: Create user
        # TODO: set zsh as the shell

      - name: Add ssh keys and config

      - name: Retrieve dotfiles config
  # TODO: Add ssh keys from a given location or generate them
  #
